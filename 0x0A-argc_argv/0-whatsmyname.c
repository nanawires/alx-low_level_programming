#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: Number of command line arguments
 * @argv: display my name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
