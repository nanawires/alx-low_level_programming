#include<stdio.h>
/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int numba;

	for (numba = '0'; numba <= '9'; numba++)
	putchar(numba);

	putchar('\n');
	return (0);
}
